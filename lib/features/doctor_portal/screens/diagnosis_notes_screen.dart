import 'package:flutter/material.dart';

class DiagnosisNotesScreen extends StatelessWidget {
  const DiagnosisNotesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Diagnosis Notes')),
      body: const Center(child: Text('Diagnosis Notes screen placeholder')),
    );
  }
}
